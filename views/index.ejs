<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Digital Audio Archive</title>
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Digital Audio Archive</title>
  <style>
    .custom-file-input {
      display: none;
    }
    .custom-file-label {
      display: inline-block;
      padding: 6px 12px;
      cursor: pointer;
      background-color: #007bff;
      color: white;
      border-radius: 4px;
    }
  </style>
</head>
<body>
  <h1>Digital Audio Archive</h1>
  
  <!-- Description of the web app -->
  <p>This web application allows users to upload, view, and manage their audio files. Users can add new audio files by providing a title, artist, and selecting an audio file to upload. The uploaded files are displayed in a list with options to play or remove them.</p>
  
  <form action="/upload" method="post" enctype="multipart/form-data">
    <input type="text" name="title" placeholder="Title" required>
    <input type="text" name="artist" placeholder="Artist" required>
    <input type="file" name="audioFile" accept="audio/*" class="custom-file-input" id="audioFile" required>
    <label for="audioFile" class="custom-file-label">Choose the file</label>
    <button type="submit">Upload Audio</button>
  </form>

  <h2>Audio Files</h2>
  <ul>
    <% files.forEach(file => { %>
      <li>
        <strong>Title:</strong> <%= file.title %> <br>
        <strong>Artist:</strong> <%= file.artist %> <br>
        <audio controls>
          <source src="/<%= file.file_path %>" type="audio/mpeg">
          Your browser does not support the audio element.
        </audio>
        <form action="/remove/<%= file.id %>" method="post" style="display:inline;">
          <button type="submit">Remove</button>
        </form>
      </li>
    <% }) %>
  </ul>

  <!-- Add your credentials at the bottom of the page -->
  <footer>
    <p>Created by Michele Grimaldi</p>
    <p>Email: <a href="mailto:mikgrimaldi@gmail.com">mikgrimaldi@gmail.com</a></p>
  </footer>
</body>
</html>





